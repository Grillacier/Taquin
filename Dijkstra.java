/*public class Dijkstra {
    //private FilePriorite F; //a implementer
    private Configuration parent; //avant-dernier sommet
    private Configuration[] pi; //sommet precedent
    private Configuration[] parcouru; //sommets visites
    private int[] d; //priorite d'un element

    public Configuration getParent() {
        return parent;
    }

    public void setParent(Configuration parent) {
        this.parent = parent;
    }

    //a modifier avec les files de priorite
    //poids d'un arc
    private int w(int u, int v) {
        if (u == v)
            return 0;
        else
            return 1;
    }

}
*/